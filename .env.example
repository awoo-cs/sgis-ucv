# Base de datos
POSTGRES_DB=sgis_db
POSTGRES_USER=sgis_user
POSTGRES_PASSWORD=sgis_pass
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Django
SECRET_KEY=cambia-esta-clave-secreta-en-produccion
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend

# JWT
ACCESS_TOKEN_LIFETIME_MINUTES=60
REFRESH_TOKEN_LIFETIME_DAYS=7

# Ingesta de eventos / mini-SIEM (V1.1)
INGEST_API_KEY=dev-sensor-key-change-me
INGEST_BLACKLIST=203.0.113.66,198.51.100.7

# Puerto del backend en el host (evita choque con 8000, p.ej. memanto)
BACKEND_PORT=8001

# Frontend — en local el navegador usa el proxy de Vite (/api → backend:8000),
# así no depende del puerto host. En producción lo setea Vercel.
# VITE_API_BASE_URL=
